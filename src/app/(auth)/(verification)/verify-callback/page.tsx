export default function VerifyCallbackPage() {
  // Tutta la logica di verifica è stata spostata nel layout auth superiore
  // Questa pagina serve solo come endpoint per il routing
  return <></>;
}